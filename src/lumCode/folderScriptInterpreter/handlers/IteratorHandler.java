package lumCode.folderScriptInterpreter.handlers;

public class IteratorHandler {

	public void interpret(int number, String rule, String[] script) {
		if (rule.matches(regex)) {

		} else {

		}
	}
}
